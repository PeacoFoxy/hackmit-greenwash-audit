# 复制成 .env 并填入真实值。.env 已被 .gitignore 排除，不会进仓库。
# 这是本项目唯一需要的凭据。

# Anthropic API key —— 只有两件事需要它：
#   1. 上传新 PDF 时的 claim 抽取
#   2. 重跑 src.extract / src.evaluate / src.coverage / src.trajectory
# 预加载的三份报告和整个界面在没有它的情况下完全可用（全部命中 cache/）。
ANTHROPIC_API_KEY=
